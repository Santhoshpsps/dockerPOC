
# Cassandra Connection Settings
spring.cassandra.contact-points=localhost
spring.cassandra.keyspace=mykeyspace
spring.cassandra.schema-action=create_if_not_exists
spring.cassandra.username=cassandra
spring.cassandra.password=cassandra
spring.cassandra.port=9042
spring.cassandra.local-datacenter=datacenter1

# Kafka Configuration
spring.kafka.bootstrap-servers=kafka:9092
spring.kafka.producer.bootstrap-servers=kafka:9092
spring.kafka.consumer.bootstrap-servers=kafka:9092
spring.kafka.consumer.group-id=my-group
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.apache.kafka.common.serialization.StringDeserializer


#docker run -d -p 9092:9092 --name kafka apache/kafka:latest
#docker run --name cassandra -d -p 9042:9042 cassandra:latest

#CREATE KEYSPACE IF NOT EXISTS mykeyspace WITH replication = {'class': 'SimpleStrategy', 'replication_factor': 1};
#CREATE TABLE IF NOT EXISTS mykeyspace.user (id TEXT PRIMARY KEY, name TEXT, email TEXT);
#INSERT INTO mykeyspace.user (id, name, email) VALUES ('1', 'John Doe', 'john.doe@example.com');

# Other Spring Boot properties (optional)
server.port=8080
logging.level.org.springframework.data.cassandra=DEBUG
